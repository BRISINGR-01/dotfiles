#!/bin/bash

name=$1

if [ -z "$name" ]; then
  echo "Usage: add-script <name>"
  exit 1
fi

if [[ $2 == "bin" ]]; then
  echo "#!/bin/bash" >~/dotfiles/bin/$name
  chmod +x ~/dotfiles/bin/$name
else
  echo "#!/bin/bash" >~/dotfiles/scripts/$name
  chmod +x ~/dotfiles/scripts/$name
fi
